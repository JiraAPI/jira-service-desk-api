#!/bin/bash
source ./set-jira-params
pushd . > /dev/null
cd ../..

LOG=logs/login.html
#read -p "\nPress [Enter] key to continue..."
curl -v ${JIRA_HOST} --user ${JIRA_USER_API_TOKEN} \
  | tee ${LOG}

#curl --request GET \
#  --url 'https://procore-test.atlassian.net/rest/servicedeskapi/knowledgebase/article?query=article' \
#  --header 'X-ExperimentalApi: opt-in' \
#  --header 'Authorization: Basic ' ${base64_TOKEN} \
#  --header 'Accept: application/json'

popd > /dev/null

# ---
#curl --request POST
#  --url 'https://<your-jira-cloud-instance>/rest/api/3/issue'
#  --header 'Authorization: Basic YourEncodedStringHere'
#  --header 'Accept: application/json'
#  --header 'Content-Type: application/json'
#  --data '{…}'
